#include<stdio.h>
void main()
{
  int n,i;
  long long factorial=1;
  printf("Enter no:");
  scanf("%d",&n);
  for(i=1;i<=n;i++)
  {
    factorial=factorial*i;
  }
  printf("Factorial of %d=%lld",n,factorial);
}
